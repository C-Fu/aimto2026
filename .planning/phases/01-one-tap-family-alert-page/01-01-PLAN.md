---
phase: 01-one-tap-family-alert-page
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, style.css]
autonomous: true
requirements: [ALRT-01, ALRT-02, RECV-02]
must_haves:
  truths:
    - "The page loads as a static single HTML file with a sender section (preset grid, custom message input, send button, status line) and a receiver section (popup container, keep-open hint)"
    - "The page is mobile-first and emoji-friendly: single column, ≥48px tap targets, system emoji fonts"
  artifacts:
    - path: "index.html"
      provides: "Complete static DOM shell (all element IDs contract for plans 02/03)"
      contains: "id=\"preset-grid\""
    - path: "style.css"
      provides: "UI-SPEC design tokens and component styles"
      contains: "--accent: #F97316"
  key_links:
    - from: "index.html"
      to: "style.css"
      via: "<link rel=\"stylesheet\" href=\"style.css\">"
      pattern: "style\\.css"
    - from: "index.html"
      to: "script.js"
      via: "<script type=\"module\" src=\"script.js\"></script>"
      pattern: "type=\"module\""
---

<objective>
Create the static page shell: `index.html` (complete DOM structure with the full element-ID contract) and `style.css` (UI-SPEC design system implementation).

Purpose: This is the UI foundation every other plan wires into. Plan 02 hydrates the preset grid and form behavior; Plan 03 uses the popup container and receiver hint. The element IDs defined here ARE the interface contract — plans 02 and 03 reference them by exact ID.
Output: `index.html` + `style.css` at repo root (GitHub Pages serves repo root).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-one-tap-family-alert-page/01-UI-SPEC.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md

**Environment note (IMPORTANT):** Project files live at `\\wsl.localhost\Debian\home\c-fu\aimto` (WSL: `/home/c-fu/aimto`). Run shell verification via `wsl -e bash -c "..."`. WSL has `curl` and `python3`; there is NO node/npm — do not attempt npm/vitest. New source files go at the repo ROOT (flat layout, per STRUCTURE.md): `index.html`, `style.css`, `script.js`, `config.js`, `ntfy-client.js`.

<interfaces>
<!-- THE ELEMENT ID CONTRACT. Plans 02/03 import these exact IDs — do not rename. -->
From index.html (to be created):
- #app-title       — h1, app title (e.g. "Family Alerts")
- #preset-grid     — empty <div> container; plan 02 renders preset buttons into it
- #custom-form     — <form> wrapping custom message input + send button
- #custom-input    — text <input>, maxlength=200, emoji allowed
- #char-count      — <span> character counter (visible only >50% of limit)
- #send-btn        — submit <button> "Send"
- #status          — <p> status line, aria-live="polite"
- #receiver-hint   — <p> "Keep this tab open…" hint
- #alert-popup     — hidden overlay container (plan 03 shows/hides it); must exist with class .alert-popup for styling
- #popup-title     — popup title element (inside #alert-popup)
- #popup-message   — popup message body (inside #alert-popup)
- #popup-close     — "OK" dismiss button (inside #alert-popup)

From style.css (to be created) — CSS custom properties consumed by later styles:
:root { --bg: #FFF8F0; --card: #FFFFFF; --accent: #F97316; --destructive: #DC2626;
        --text: #1C1917; --text-muted: #78716C; --success: #16A34A;
        --popup-bg: #1C1917; --border: #E7E5E4;
        --space-xs: 4px; --space-sm: 8px; --space-md: 16px; --space-lg: 24px; --space-xl: 32px; --space-2xl: 48px;
        --radius: 12px; --font-stack: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        --font-emoji: "Apple Color Emoji", "Segoe UI Emoji", "Noto Color Emoji"; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: create index.html with full DOM shell</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/01-one-tap-family-alert-page/01-UI-SPEC.md (design contract — copy, structure, accessibility)
    - .planning/codebase/CONVENTIONS.md (naming, structure conventions)
  </read_first>
  <action>
    Create `index.html` at repo root — a complete static HTML5 document implementing the UI-SPEC contract. Exact requirements:

    1. `<head>`: `<!DOCTYPE html>`, `<html lang="en">`, `<meta charset="utf-8">`, `<meta name="viewport" content="width=device-width, initial-scale=1">`, `<title>Family Alerts</title>`, `<link rel="stylesheet" href="style.css">`.
    2. Body structure (semantic tags, one `<main>`):
       - `<header>` with `<h1 id="app-title">Family Alerts 👨‍👩‍👧</h1>`
       - `<section id="sender">` with `<h2>Send an alert</h2>`
         - `<div id="preset-grid"></div>` — MUST be empty (plan 02 renders buttons from config.js; do not hardcode buttons here)
         - `<form id="custom-form">` containing:
           - `<label for="custom-input">Custom message</label>`
           - `<input type="text" id="custom-input" maxlength="200" placeholder="Type a short message… (up to 200 characters)" autocomplete="off">`
           - `<span id="char-count" aria-hidden="true"></span>`
           - `<button type="submit" id="send-btn">Send</button>`
         - `<p id="status" aria-live="polite" role="status"></p>`
       - `<section id="receiver">` with `<h2>Receiving alerts</h2>`
         - `<p id="receiver-hint">Keep this tab open to receive alerts. Or install the ntfy app and subscribe to your topic.</p>`
       - `<div id="alert-popup" class="alert-popup" role="alertdialog" aria-labelledby="popup-title" aria-hidden="true" hidden>` (overlay, rendered on top of everything):
         - `<h3 id="popup-title"></h3>`
         - `<p id="popup-message"></p>`
         - `<button type="button" id="popup-close">OK</button>`
       - `<script type="module" src="script.js"></script>` — placed at end of body (file does not exist yet; the 404 in this wave is expected and harmless — plan 02 creates it).
    3. Accessibility: every interactive element is a real `<button>`/`<input>`; labels linked via `for`/`id`.
    4. NO inline styles, NO inline event handlers (onclick etc.), NO hardcoded preset buttons — all styling in style.css, all behavior in script.js (plan 02).
    5. `#char-count` must be styled-hidden by default (plan 02 JS shows it when input exceeds 50% of 200).
    6. Do NOT include any ntfy topic name anywhere in this file.
  </action>
  <acceptance_criteria>
    - `index.html` exists and contains ALL of: `id="app-title"`, `id="preset-grid"`, `id="custom-form"`, `id="custom-input"`, `maxlength="200"`, `id="send-btn"`, `id="status"`, `id="receiver-hint"`, `id="alert-popup"`, `id="popup-title"`, `id="popup-message"`, `id="popup-close"`
    - `index.html` contains `<link rel="stylesheet" href="style.css">` and `<script type="module" src="script.js"></script>`
    - `index.html` contains `role="status"` on `#status` and `role="alertdialog"` on `#alert-popup`
    - `grep -c "onclick" index.html` returns 0
    - `grep -c "ntfy" index.html` returns 0
    - `#preset-grid` div is empty: `<div id="preset-grid"></div>`
  </acceptance_criteria>
  <verify>
    <automated>wsl -e bash -c "cd /home/c-fu/aimto && grep -c 'id=\"preset-grid\"' index.html && grep -c 'maxlength=\"200\"' index.html && grep -c onclick index.html && grep -c ntfy index.html"</automated>
  </verify>
  <done>index.html exists with the complete element-ID contract; no inline handlers; no topics embedded; all grep gates pass.</done>
</task>

<task type="auto">
  <name>task 2: create style.css implementing the UI-SPEC design system</name>
  <files>style.css</files>
  <read_first>
    - .planning/phases/01-one-tap-family-alert-page/01-UI-SPEC.md (spacing/typography/color/interaction contract)
    - index.html (elements being styled — just created in task 1)
  </read_first>
  <action>
    Create `style.css` at repo root implementing EVERY token and rule from the UI-SPEC. Exact requirements:

    1. `:root` custom properties block exactly as declared in the `<interfaces>` block above (colors, spacing scale, --radius: 12px, font stacks).
    2. Base: `box-sizing: border-box` on `*`; `body` uses `--font-stack` with `--font-emoji` as fallback, `background: var(--bg)`, `color: var(--text)`, `font-size: 16px`, `line-height: 1.5`, `margin: 0`.
    3. Layout: `#app-title`, sections, and form inside a centered content wrapper — use `.container { max-width: 480px; margin: 0 auto; padding: var(--space-2xl) var(--space-md); }` applied via a wrapping div in... DO NOT modify index.html for this; instead style `main` directly: `main { max-width: 480px; margin: 0 auto; padding: var(--space-2xl) var(--space-md); }`.
    4. Typography per UI-SPEC table: `#app-title` = 32px/700/1.2; `h2` = 24px/600/1.3; buttons = 18px/600; body 16px/1.5; labels/hints 14px/500.
    5. Buttons: `min-height: 48px` (tap target), `border-radius: var(--radius)`, `font-size: 18px`, `font-weight: 600`, `cursor: pointer`, `border: none`. Focus ring: `:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }`.
    6. `#preset-grid`: `display: grid; grid-template-columns: 1fr; gap: var(--space-sm);` and at `@media (min-width: 400px)` switch to `grid-template-columns: 1fr 1fr;`.
    7. Preset buttons: idle = `background: var(--card); color: var(--text); border: 1px solid var(--border);` — white card look per UI-SPEC (accent is reserved for active/pressed states, NOT idle). Emoji in button text renders via `--font-emoji`. Pressed/disabled-with-success state uses accent: `.preset-btn:active { background: var(--accent); color: #fff; }`.
    8. `#send-btn`: `background: var(--accent); color: #fff; width: 100%; margin-top: var(--space-md);` (primary CTA — accent IS its resting state).
    9. `#custom-form`: `display: flex; flex-direction: column; gap: var(--space-sm);`. `#custom-input`: `padding: var(--space-md); font-size: 16px; border: 1px solid var(--border); border-radius: var(--radius); font-family: inherit;`.
    10. `#char-count`: hidden by default (`display: none`; `color: var(--text-muted); font-size: 14px;`); class `.visible` shows it (`display: inline;`) — plan 02 toggles this.
    11. `#status`: `font-size: 14px; min-height: 21px;` (reserve space); classes `.status-success { color: var(--success); }` and `.status-error { color: var(--destructive); }` — plan 02 applies them.
    12. `#alert-popup` overlay: `position: fixed; inset: 0; display: flex; align-items: center; justify-content: center; background: rgba(0,0,0,0.5); z-index: 1000; padding: var(--space-lg);` and inner card styling via `.alert-popup > div` — DO NOT change the popup DOM in index.html; add the inner wrapper styles: `.alert-popup { }` . To avoid a DOM change, style the children directly: `#alert-popup { position: fixed; ... } #popup-title, #popup-message { color: #fff; } #alert-popup #popup-close { background: var(--accent); color: #fff; padding: var(--space-sm) var(--space-lg); border-radius: var(--radius); border: none; min-height: 48px; font-size: 18px; cursor: pointer; }`. Add `[hidden] { display: none !important; }` so the popup stays hidden until plan 03 removes the attribute.
    13. Motion: `@media (prefers-reduced-motion: no-preference)` — popup uses a fade/slide-in transition (`@keyframes popup-in { from { opacity: 0; transform: translateY(12px); } to { opacity: 1; transform: translateY(0); } }` applied to popup children when visible). Under `prefers-reduced-motion: reduce`, disable animations/transitions (per UI-SPEC interaction contract).
    14. 2-space indentation, UTF-8, LF endings.
  </action>
  <acceptance_criteria>
    - `style.css` contains `:root` with ALL of: `--bg: #FFF8F0`, `--accent: #F97316`, `--destructive: #DC2626`, `--success: #16A34A`, `--popup-bg: #1C1917`
    - `style.css` contains `min-height: 48px` on button rules
    - `style.css` contains `@media (min-width: 400px)` and `grid-template-columns: 1fr 1fr`
    - `style.css` contains `prefers-reduced-motion` (both reduce and no-preference variants)
    - `style.css` contains `.status-error` with `--destructive` and `.status-success` with `--success`
    - `style.css` contains `#alert-popup` with `position: fixed`
    - `grep -c "style.css" index.html` returns 1 (link present)
  </acceptance_criteria>
  <verify>
    <automated>wsl -e bash -c "cd /home/c-fu/aimto && grep -c '\-\-accent: #F97316' style.css && grep -c 'min-height: 48px' style.css && grep -c 'prefers-reduced-motion' style.css && grep -c 'position: fixed' style.css && grep -c 'style.css' index.html"</automated>
  </verify>
  <done>style.css implements the full UI-SPEC: tokens, 48px tap targets, responsive preset grid, popup overlay, reduced-motion support. All grep gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page DOM → browser rendering | Static markup only in this plan; untrusted ntfy message content is NOT yet rendered (arrives in plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering (XSS) | future popup rendering (`#alert-popup`) | mitigate | This plan creates empty containers only. Plan 03 MUST render incoming messages with `textContent` only — grep gate `grep -c innerHTML script.js == 0` enforced in plan 03. |
| T-01-02 | Information Disclosure | index.html/style.css | mitigate | No topic names, secrets, or personal info embedded in markup (grep gate: `grep -c ntfy index.html == 0`). Topics live in config.js (plan 04). |
| T-01-03 | Spoofing | static page (no auth by design) | accept | Open access is a locked design decision (PROJECT.md). Spoofing is only possible if the topic leaks; topic rotation documented in plan 04 README. |
</threat_model>

<verification>
Phase checks for plan 01:
1. All element-ID contract elements exist in index.html (grep gates in tasks)
2. style.css contains every UI-SPEC token (grep gates in tasks)
3. Page renders: `wsl -e bash -c "cd /home/c-fu/aimto && (python3 -m http.server 8090 &> /dev/null &) ; sleep 1; curl -s http://localhost:8090/index.html | grep -c 'preset-grid'; kill %1"` returns ≥1
</verification>

<success_criteria>
- `index.html` and `style.css` exist at repo root with the full element-ID contract and UI-SPEC design system
- Page is static-GitHub-Pages-compatible (no build step, no server includes)
- Sender UI (preset grid container, custom form, status) and receiver UI (popup overlay, hint) are present and styled
- All task grep gates pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-one-tap-family-alert-page/01-01-SUMMARY.md`
</output>

---
phase: 01-one-tap-family-alert-page
plan: 01-04
type: execute
wave: 1
depends_on: []
files_modified: [config.js, README.md, .editorconfig, .gitignore, .nojekyll]
autonomous: true
requirements: [CFG-01, CFG-02]
must_haves:
  truths:
    - "Each sender/receiver pair's ntfy topic is configurable in exactly one place (per-deployment config constant)"
    - "The site can be deployed to GitHub Pages as pure static files with documented steps"
    - "A new family member can onboard by following README instructions (topic setup + ntfy app subscription)"
  artifacts:
    - path: "config.js"
      provides: "Single source of truth: CONFIG.TOPIC, CONFIG.MAX_MESSAGE_LENGTH, PRESETS array (CFG-01)"
      contains: "export const CONFIG"
    - path: "README.md"
      provides: "Onboarding, topic setup, ntfy app subscribe steps, GitHub Pages deploy steps (CFG-02, RECV-01 onboarding)"
      contains: "GitHub Pages"
    - path: ".nojekyll"
      provides: "GitHub Pages static-serving marker"
  key_links:
    - from: "config.js"
      to: "script.js"
      via: "ES module import (plan 02)"
      pattern: "from './config.js'"
    - from: "README.md"
      to: "config.js"
      via: "Topic replacement instructions ('replace this topic')"
      pattern: "config.js"
---

<objective>
Create the deployment/configuration foundation: `config.js` (single source of truth for topic + presets + limits — the interface contract plans 02 and 03 import), plus static-site hygiene files (`.editorconfig`, `.gitignore`, `.nojekyll`) and the onboarding README with GitHub Pages deploy steps.

Purpose: CFG-01 (per-pair topic configurable per deployment) and CFG-02 (static GitHub Pages deploy) live here. The config.js export shapes ARE the contract for plans 02/03.
Output: `config.js`, `README.md`, `.editorconfig`, `.gitignore`, `.nojekyll` at repo root.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/INTEGRATIONS.md
@IDEA.md

**Environment note:** Project files live at `\\wsl.localhost\Debian\home\c-fu\aimto` (WSL: `/home/c-fu/aimto`). Shell verification via `wsl -e bash -c "..."`. No node/npm in this environment. Flat repo-root layout.

<interfaces>
<!-- THE config.js CONTRACT. Plans 02/03 import these EXACT export names — do not rename. -->
From config.js (to be created):

```javascript
export const CONFIG = {
  TOPIC: "fam-alerts-demo-2026",        // CFG-01: per-deployment topic — CHANGE THIS to a random unguessable topic (see README)
  MAX_MESSAGE_LENGTH: 200,              // ALRT-02 limit, enforced in plan 02
  SENDER_NAME: "Family",                // used as custom-message title prefix (plan 02)
};

export const PRESETS = [
  // { id, emoji, label (button text), title (ntfy X-Title), priority (ntfy X-Priority: default|high|urgent) }
  { id: "school", emoji: "🚗", label: "Dad arrived at school",   title: "🚗 Dad arrived",   priority: "default" },
  { id: "food",   emoji: "🍲", label: "Food ready, come down!",  title: "🍲 Food ready",    priority: "high" },
  { id: "rain",   emoji: "🌧️", label: "Rain coming, take in the clothes", title: "🌧️ Rain coming", priority: "urgent" },
];
```

- ntfy API notes (from INTEGRATIONS.md + docs.ntfy.sh): publish via `POST https://ntfy.sh/{topic}`; headers `Title`/`X-Title`, `Priority`/`X-Priority` (values `min|low|default|high|urgent|max` or 1-5), `Tags`/`X-Tags` (emoji shortcodes). Topic charset `[-_A-Za-z0-9]`, ≤64 chars. Priority is what selects the sound on the Android app (custom sounds map to priorities) — this is how ALRT-04 "sender's chosen sound" works.
- Topic secrecy (CONCERNS.md): topics are the only access control. Committed topic is a demo placeholder; README must document generating a random topic (ntfy.sh topic generator) and replacing it. Do NOT invent personal/guessable topic names in config.js.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: create config.js — single source of truth</name>
  <files>config.js</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (module conventions, JSDoc, config.js as single source of truth)
    - .planning/codebase/CONCERNS.md (topic secrecy: random unguessable topics, never commit real topics)
  </read_first>
  <action>
    Create `config.js` at repo root implementing EXACTLY the `<interfaces>` contract above. Requirements:

    1. `export const CONFIG = { TOPIC: "fam-alerts-demo-2026", MAX_MESSAGE_LENGTH: 200, SENDER_NAME: "Family" }` — exact keys/values.
    2. `export const PRESETS = [...]` — the 3 presets from the contract: `{ id, emoji, label, title, priority }` with priority values exactly `"default"`, `"high"`, `"urgent"`. These are the demo defaults from IDEA.md (🚗 school / 🍲 food / 🌧️ rain) — a family forks and edits this array.
    3. JSDoc on every export:
       - `@typedef {Object} Preset` — `{string} id`, `{string} emoji`, `{string} label`, `{string} title`, `{string} priority` (one of "default"|"high"|"urgent")
       - On `CONFIG`: document that `TOPIC` is the per-pair ntfy topic (CFG-01), that the value is a placeholder for the demo and MUST be replaced with a random topic per the README before real family use, and that it must contain only `[-_A-Za-z0-9]` (no leading `ntfy.sh/`).
       - On `PRESETS`: document that each preset maps to a button; `priority` selects the receiver-side sound (ntfy maps priorities to sounds).
    4. Plain ES module — no imports, no side effects at module load (no fetch/console on import). `const` only, camelCase.
    5. Comment (per CONVENTIONS "comment non-obvious logic only"): one comment above TOPIC explaining "topic is the shared secret — change per pair, keep unguessable".
  </action>
  <acceptance_criteria>
    - `config.js` contains `export const CONFIG` and `export const PRESETS`
    - `config.js` contains `TOPIC: "fam-alerts-demo-2026"` and `MAX_MESSAGE_LENGTH: 200`
    - `config.js` contains exactly 3 preset entries with ids `school`, `food`, `rain` and priorities `"default"`, `"high"`, `"urgent"`
    - `config.js` contains `@typedef {Object} Preset`
    - `config.js` does NOT contain `ntfy.sh/` (topic stored without scheme, per contract)
    - `grep -c "import" config.js` returns 0
  </acceptance_criteria>
  <verify>
    <automated>wsl -e bash -c "cd /home/c-fu/aimto && grep -c 'export const CONFIG' config.js && grep -c 'TOPIC' config.js && grep -c 'school' config.js && grep -c 'ntfy.sh/' config.js; echo '---'; grep -c 'import' config.js"</automated>
  </verify>
  <done>config.js exists with CONFIG + PRESETS exports matching the contract exactly; zero module side effects; topic placeholder documented.</done>
</task>

<task type="auto">
  <name>task 2: create README.md + static-site hygiene files</name>
  <files>README.md, .editorconfig, .gitignore, .nojekyll</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (.editorconfig prescriptive spec)
    - .planning/codebase/CONCERNS.md (onboarding checklist requirement, known limitations to document)
    - .planning/codebase/INTEGRATIONS.md (ntfy + GitHub Pages integration facts)
    - config.js (topic name referenced in README)
  </read_first>
  <action>
    Create four files at repo root:

    **1. `README.md`** — onboarding + deploy guide. MUST contain these sections (exact headings):
    - `## What this is` — one-paragraph: static single-page family alert site; sender taps a preset or types a short emoji message; delivered to the family's ntfy topic; receiver sees a push (ntfy app) or an in-page popup with sound (open browser tab). No backend, no accounts.
    - `## How it works` — send path (browser POST → https://ntfy.sh/<topic>) and receive path (ntfy app push OR in-page SSE subscription + popup + sound). Mention: "The browser tab must stay open to receive in-page alerts."
    - `## Setup: your own family topic` — numbered steps: (1) generate a random unguessable topic using the generator at https://ntfy.sh (or any random string matching `[-_A-Za-z0-9]`, ≤64 chars — "the topic is the password: do not use family names"); (2) open `config.js` and replace `CONFIG.TOPIC` with your topic; (3) commit and deploy. Include a "topic rotation" note: to revoke a leaked topic, change it here and re-deploy — every device must use the new value.
    - `## Receiver setup (ntfy app)` — numbered steps for RECV-01: install the ntfy app (Android/iOS), subscribe to your topic (`ntfy.sh/<your-topic>`), enable notifications + sound in app settings. Note: high/urgent alerts use louder sounds — custom per-priority sounds are configurable in the app.
    - `## Run locally` — `python3 -m http.server 8000` then open http://localhost:8000.
    - `## Deploy to GitHub Pages` — numbered steps (CFG-02): push this repo to GitHub → repo Settings → Pages → Source: "Deploy from a branch" → branch `master` `/ (root)` → Save → wait for the Pages build → open the published URL. Note the site is 100% static — no build command, no CI config needed.
    - `## Known limitations` — bullet list (from CONCERNS.md): open access (anyone with the topic can send — keep it secret); alerts are fire-and-forget (no history); in-page alerts require an open tab; delivery depends on the free ntfy.sh service.
    - `## Demo verification checklist` — a smoke checklist: (1) open local server; (2) tap a preset — hear confirmation sound, status shows "Sent ✓"; (3) open https://ntfy.sh/<topic> in another tab — message with title appears; (4) with the page open + subscribed, send from another device — popup + sound; (5) ntfy app receives push.

    **2. `.editorconfig`** — exact content:
    ```
    root = true

    [*]
    charset = utf-8
    indent_style = space
    indent_size = 2
    end_of_line = lf
    insert_final_newline = true
    ```

    **3. `.gitignore`** — OS/editor junk only:
    ```
    .DS_Store
    Thumbs.db
    *.local.html
    ```
    Do NOT ignore config.js (it is committed — the demo topic placeholder is intentional).

    **4. `.nojekyll`** — empty file (marker that tells GitHub Pages to skip Jekyll processing).
  </action>
  <acceptance_criteria>
    - `README.md` contains `## Setup: your own family topic` and `## Deploy to GitHub Pages` and `## Receiver setup (ntfy app)` and `## Known limitations`
    - `README.md` contains `config.js` (topic replacement reference) and `python3 -m http.server 8000`
    - `README.md` contains `GitHub Pages` (appears in deploy section) and `https://ntfy.sh/` (at least once)
    - `.editorconfig` contains `indent_size = 2` and `charset = utf-8`
    - `.gitignore` exists and does NOT contain `config.js`
    - `.nojekyll` exists and is empty (0 bytes)
  </acceptance_criteria>
  <verify>
    <automated>wsl -e bash -c "cd /home/c-fu/aimto && grep -c '## Deploy to GitHub Pages' README.md && grep -c '## Setup: your own family topic' README.md && grep -c 'python3 -m http.server 8000' README.md && stat -c%s .nojekyll && grep -c 'config.js' .gitignore; echo '---'; grep -c 'indent_size = 2' .editorconfig"</automated>
  </verify>
  <done>README documents topic setup, ntfy app onboarding, local run, GitHub Pages deploy, limitations, and a verification checklist. Hygiene files present and correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo (public GitHub) → ntfy topic | Committed config.js exposes the topic name to anyone with repo access |
| README instructions → user devices | Misconfiguration (wrong topic) silently breaks delivery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | config.js TOPIC | mitigate | Committed value is an obvious demo placeholder (`fam-alerts-demo-2026`). README mandates generating a random unguessable topic before real family use and documents rotation. |
| T-04-02 | Spoofing | ntfy topic (open access) | accept | Locked design decision (no auth, PROJECT.md). Topic-as-secret documented in README; risk surface limited to demo scope. |
| T-04-03 | Denial of Service | public ntfy.sh dependency | accept | Single external dependency by design; README Known limitations documents the dependency. Fetch retry handled in plan 02. |
| T-04-04 | Tampering | README instructions | accept | Static docs, no executable instructions beyond standard setup steps. |
</threat_model>

<verification>
1. All grep gates in tasks pass
2. `wsl -e bash -c "cd /home/c-fu/aimto && ls -la config.js README.md .editorconfig .gitignore .nojekyll"` — all 5 files present
3. config.js parses as valid JS module: `wsl -e bash -c "cd /home/c-fu/aimto && python3 -c \"import re; s=open('config.js').read(); assert 'export const CONFIG' in s and 'export const PRESETS' in s; print('OK')\""`
</verification>

<success_criteria>
- Topic configurable per deployment in exactly one file (config.js CONFIG.TOPIC) — CFG-01
- README fully documents GitHub Pages static deployment — CFG-02
- README documents ntfy app onboarding (supports RECV-01 delivery path)
- Files are GitHub-Pages compatible (static, .nojekyll marker, no build step)
</success_criteria>

<output>
After completion, create `.planning/phases/01-one-tap-family-alert-page/01-04-SUMMARY.md`
</output>
